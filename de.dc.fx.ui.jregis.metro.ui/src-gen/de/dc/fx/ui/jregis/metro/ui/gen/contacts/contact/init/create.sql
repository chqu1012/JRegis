CREATE TABLE IF NOT EXISTS CONTACT(ID BIGINT AUTO_INCREMENT, FIRSTNAME VARCHAR, LASTNAME VARCHAR, USERNAME VARCHAR, CONTACT_IMAGE_ID VARCHAR, CONTACT_GROUP_ID VARCHAR, STATUS BIGINT, CREATED_ON TIMESTAMP, UPDATED_ON TIMESTAMP);
